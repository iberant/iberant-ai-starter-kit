interface:
  display_name: "API Conventions"
  short_description: "Review API implementations against established conventions"
  default_prompt: "Use $api-conventions to review this API implementation and return a revised version with convention violations highlighted."
