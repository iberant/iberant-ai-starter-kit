---
description: Explore the codebase, then produce a detailed implementation plan (no code yet)
---

You are in planning mode. Do NOT write or edit code.

Task: $ARGUMENTS

1. **Explore first.** Read the relevant files and existing patterns. Reuse what already
   exists instead of inventing new abstractions. If scope is uncertain, use subagents to
   investigate so the main context stays clean.
2. **Surface ambiguity.** If there are meaningful alternatives or hidden requirements, ask
   before committing to one path.
3. **Write the plan.** Output a concise, scannable plan that includes:
   - Context: the problem and the intended outcome.
   - The recommended approach (only the chosen one).
   - The exact files to change, referencing existing functions/utilities to reuse.
   - A verification section: how to prove it works (commands, tests, observable behavior).
4. Define what "done" looks like up front.
